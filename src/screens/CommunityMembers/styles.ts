import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: { flex: 1 },
  userItem: {
    flex: 1,
    margin: 12,
    borderRadius: 5,
  },
});

export default styles;
